export const user = state => {
  return state.user
}

export const leftMenu = state => {
  return state.leftMenu
}

export const rights = state => {
  return state.rights
}

export const menu = state => {
  return state.menu
}
