export const user = state => {
  return state.user
}

export const edit = state => {
  return state.edit
}

export const leftmenu = state => {
  return state.leftmenu
}

export const semesterList = state => {
  return state.semesterList
}

export const sectionList = state => {
  return state.sectionList
}
