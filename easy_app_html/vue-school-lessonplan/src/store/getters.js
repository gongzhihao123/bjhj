export const path = state => {
  return state.path
}

export const leftMenu = state => {
  return state.leftMenu
}

export const right = state => {
  return state.rights
}

export const user = state => {
  return state.user
}

export const edit = state => {
  return state.edit
}

export const defaultActive = state => {
  return state.defaultActive
}

export const previewUrl = state => {
  return state.previewUrl
}
