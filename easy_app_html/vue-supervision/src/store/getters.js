export const user = state => {
  return state.user
}

export const leftmenu = state => {
  return state.leftmenu
}

export const rootUrl = state => {
  return state.rootUrl
}

export const userType = state => {
  return state.userType
}

export const previewUrl = state => {
  return state.previewUrl
}

export const previewInterface = state => {
  return state.previewInterface
}

export const schoolYear = state => {
  return state.schoolYear
}

export const leaderFlag = state => {
  return state.leaderFlag
}

export const areaId = state => {
  return state.areaId
}
